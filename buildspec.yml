version: 0.2

env:
  secrets-manager:
    DB_URI: "db-uri"

phases:
  install:
    runtime-versions:
      python: 3.13
    commands:
      - echo "Installing python dependencies..."
      - pip install --upgrade pip
      - pip install -r requirements.txt
      - echo "DB_URI=$DB_URI" >> .env

  # Run unit tests
  pre_build:
    commands:
      - echo "Running unit tests..."
      - pytest tests/ -v
      - echo "Unit tests passed successfully."

  # Build application (TODO)
  build:
    commands:
      - echo "Building Docker image..."
      - docker build -t myapp:$CODEBUILD_BUILD_ID .
      - echo "Saving Docker image as artifact..."
      - mkdir -p artifact
      - docker save myapp:$CODEBUILD_BUILD_ID -o artifact/app_image.tar

  post_build:
    commands:
      - echo "Build completed on `date`"
      - echo "Artifact prepared for S3 upload."

artifacts:
  files:
    - artifact/app_image.tar
  discard-paths: yes
  base-directory: artifact
